# REST-API-CRUD-with-authentication

An express server to perform CRUD operations with login and registration API for users to log into the app and register a new user, store the data in the mongodb.
